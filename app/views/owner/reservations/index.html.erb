<div class = "row">
	<h1 class = "offset-5">予約者様情報</h1><br>
</div>

<div class = "row">
	<p class = "offset-4">記載の電話番号にご連絡いただき、予約時間の確認及び予約確定のご連絡をお願いいたします。</p>
</div>

<% if @reservations.blank? %>
	<div class = "row">
		<div class = "offset-4">現在予約はありません。</div>
	</div>
<% end %>

<div class="row row-cols-1 row-cols-md-4">
  <% @reservations.each do |reservation| %>
      <div class="card border-dark">
          <div class="card-body offset-3">
            <h5 class="card-title">名前:<%= reservation.user.name %>様</h5>
            	<div class="card-text">フリガナ:<%= reservation.user.kana_name %><br>電話番号:<%= reservation.user.phone_number %><br>予約サロン名:<%= reservation.salon.salon_name %><br>予約時間:<%= reservation.date %></div>
          </div>
      </div>
  <% end %>
</div>